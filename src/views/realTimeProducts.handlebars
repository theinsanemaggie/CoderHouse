{{!-- <h1>Productos en tiempo real</h1>
<ul id="product-list"></ul>

<script src="/socket.io/socket.io.js"></script>
<script>
    const socket = io();

    socket.on("updateProducts", (products) => {
        const list = document.getElementById("product-list");
        list.innerHTML = "";
        
        <!--Si no tiene un valor asignado, se le asigna uno por defecto-->
        products.forEach(p => {
            const li = document.createElement("li");
            li.innerHTML = `
                <strong>${p.title} - $${p.price}</strong><br>
                Descripción: ${p.description || 'N/A'}<br> 
                Código: ${p.code || 'N/A'}<br>
                Stock: ${p.stock || 0}<br>
                Categoría: ${p.category || 'N/A'}<br>
                Estado: ${p.status ? 'Disponible' : 'No disponible'}<br>
                ${p.thumbnails?.length ? `
                    <p>Imágenes:</p>
                    <ul>
                        ${p.thumbnails.map(t => `<li><img src="${t}" width="100"></li>`).join('')}
                    </ul>` : ''
                }
            `;
            list.appendChild(li);
        });
    });
</script> --}}

<h1>Productos en tiempo real</h1>
<ul id="listaProductos"></ul>
<h2>Crear producto</h2>
<form id="formCrear">
  <div class="fila">
    <input id="nombre" name="nombre" placeholder="Nombre" required>
    <input id="precio" name="precio" type="number" step="0.01" placeholder="Precio" required>
  </div>
  <button type="submit">Agregar</button>
</form>
<h2>Eliminar producto</h2>
<form id="formEliminar">
  <input id="idProducto" name="idProducto" type="number" placeholder="ID" required>
  <button type="submit">Eliminar</button>
</form>
<p><a href="/">Volver al inicio</a></p>
<script src="/socket.io/socket.io.js"></script>
<script src="/js/realtime.js"></script>

